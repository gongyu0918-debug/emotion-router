interface:
  display_name: "Emotion Router"
  short_description: "Three-route soft router for coding agents when the current prompt shows urgency, anger/frustration, or confusion."
  default_prompt: "Use $emotion-skill when the current user prompt or visible context shows urgency, anger/frustration, or confusion. Read SKILL.md, then use references/emotion-routes.md for the selected route. Apply the matching prompt pattern without exposing emotion labels or running a Python classifier."
  version: "2.0.0"
  author: "gongyu0918-debug"
  license: "MIT"

policy:
  allow_implicit_invocation: true
